class Main {
    function void main() {
        var char exitKey;
        var SnakeGame game;
        let exitKey = Keyboard.keyPressed();
        while (~(exitKey = 140)){
            let game = SnakeGame.new(256,128);
            do game.start();
            do game.dispose();
            return;
        }
    }
}